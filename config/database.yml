default: &default
    adapter: postgresql
    encoding: unicode
    host: db
    username: postgres
    password: 
    pool: 5

development:
    <<: *default
    database: hackerspace_dev

test:
    <<: *default
    database: hackerspace_test
